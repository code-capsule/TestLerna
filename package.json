{
  "name": "capsule-packages",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "lerna version --yes && lerna publish from-package --force-publish --yes --registry=https://registry.npmjs.org/"
  },
  "devDependencies": {
    "lerna": "^5.6.2"
  }
}
